acceptance:
  provisioner: abs
  images: ['centos-7-x86_64']
  vars: |
    role: server
master:
  provisioner: abs
  images: ['centos-7-x86_64']
  vars: |
    role: server
agent:
  provisioner: abs
  images: ['centos-7-x86_64']
  vars: |
    role: agent
